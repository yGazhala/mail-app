'use strict';

angular
    .module('shared')
    .component('layout', {
        bindings: {},
        templateUrl: 'shared/layout.html',
        controller: LayoutController
    });

function LayoutController() {

}